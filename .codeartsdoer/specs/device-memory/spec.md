# 设备管理面内存统计优化 - 增量规格

- 只有 CPU 主进程可以查询管理面总内存，从进程必须返回失败。
- 总内存由 RTOS 固定底噪、服务固定底噪和所有活动进程上报值组成。
- 进程重复上报应覆盖旧值，不应重复累计。
- 进程退出后应删除本地记录，后续查询不再包含该进程。
- 可靠上报窗口为 5 分钟，1 秒一次，最多 300 次。
- 新增逻辑应保持 C11 可编译，并提供无需设备硬件的单元测试。

